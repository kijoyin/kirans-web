apiVersion: apps/v1
kind: Deployment
metadata:
  name: kiranjoy-web
  namespace: kiranjoy-prod
  labels:
    app: kiranjoy-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kiranjoy-web
  template:
    metadata:
      labels:
        app: kiranjoy-web
    spec:
      containers:
      - name: kiranjoy-web
        # Updated automatically by CI (.github/workflows/ci-deploy.yml)
        image: ghcr.io/kijoyin/kiranjoy-web:6306a4f50a66c4f7f97bc01e86ce72c77bee616e
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 20
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
